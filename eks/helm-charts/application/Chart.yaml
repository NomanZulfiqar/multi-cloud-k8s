apiVersion: v2
name: myapp
description: EKS Application with Auto-Sync Secrets
type: application
version: 0.1.0
appVersion: "1.0"

# Dependencies removed - CSI driver installed separately in pipeline
# PostgreSQL disabled for now